import { SlideDeck } from './components/SlideDeck';

export default function App() {
  return (
    <div className="w-full h-screen bg-[#111111] flex items-center justify-center overflow-hidden">
      <SlideDeck />
    </div>
  );
}